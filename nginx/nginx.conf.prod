server {
    listen 80;
    server_name food-service.com;
    location / {
        proxy_pass http://fradmin:3000;
    }
    location /api {
        proxy_pass http://api:3001;
        rewrite ^/api/(.*) /$1 break;
    }
}
